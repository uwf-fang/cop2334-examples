if (age > 1 && age < 10);  // still compile, but the ; here make the if statement runs with empty body
  cout << "Young child\n"; // this line will always execute because it is not part of the if statement any more